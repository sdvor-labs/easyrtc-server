extends profile-layout

block content
    each item in rooms
        div.box
            div.columns
                div.column
                    h5.title.is-5 #{item.label}
                div.column.has-text-centered
                    a.button.is-info.is-fullwidth(href=`/profile/rooms/${item.name}`) Edit
