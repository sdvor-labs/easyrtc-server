extends profile-layout

block content
    each page in pages
        div.box
            div.columns
                div.column
                    h5.title.is-5 #{page.title}
                div.column.has-text-centered
                    a.button.is-info.is-fullwidth(href=`/profile/pages/${page.name}`) Изменить